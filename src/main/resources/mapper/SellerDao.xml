<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com._401unauthorized.sheep.dao.SellerDao">

    <select id="seller_list" resultType="com._401unauthorized.sheep.dto.WishDto">
        SELECT W.WISH_NUMBER,W.USER_ID,W.WISH_TITLE FROM WISH W JOIN SELLER S WHERE W.WISH_TITLE LIKE CONCAT(S.SELLER_AREA,'%') AND S.USER_ID=#{userId};
    </select>
</mapper>