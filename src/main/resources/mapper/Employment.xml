<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com._401unauthorized.sheep.dao.EmploymentDao">

    <!--글을 쓰자!-->
    <insert id="write" useGeneratedKeys="true" keyProperty="board_number">
        INSERT INTO BOARD VALUES (null, #{user_id}, #{board_title}, #{board_content}, 0, default, 2)
    </insert>

    <insert id="insert_count">
        INSERT INTO JOB_BOARD (BOARD_NUMBER ,JOB_COUNT) VALUES (#{board_number}, #{job_count});
    </insert>

    <select id="select_area" resultType="java.lang.Boolean"></select>

    <select id="get_board_list" resultType="com._401unauthorized.sheep.dto.BoardDto">

    </select>

</mapper>